import StatusPage from "@/components/StatusPage";

export default function Done() {
  return (
    <StatusPage title="All set">
      You can close this tab. Mochi picks up the change within a minute or so, or reopen it if you
      would rather not wait.
    </StatusPage>
  );
}
